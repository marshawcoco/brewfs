# Local object storage + Postgres metadata
mount_point: /tmp/brewfs-pg

data:
  backend: local-fs
  localfs:
    data_dir: /var/lib/brewfs/data

meta:
  backend: sqlx
  sqlx:
    url: "postgres://postgres:postgres@127.0.0.1:5432/brewfs"

layout:
  chunk_size: 67108864
  block_size: 4194304
